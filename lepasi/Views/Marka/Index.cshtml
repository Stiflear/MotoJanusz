@model IEnumerable<lepasi.marka>

@*@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>*@
    <link href="~/Models/Table/Table.css" rel="stylesheet" />
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
@*</head>
<body>*@
    @*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
    <div>
        @{ Html.RenderAction("Menu", "Menu");}
    </div>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MA_nazwa)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MA_nazwa)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MA_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.MA_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MA_id })
            </td>
        </tr>
    }
    
    </table>
@*</body>
</html>*@
